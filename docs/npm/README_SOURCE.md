The npm package README source lives in `docs/npm/README.md`.

The release pipeline copies that file into the staged npm package so the npm
page stays aligned with the repository without duplicating long-form content.
